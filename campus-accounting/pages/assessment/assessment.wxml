<view class="container">
  <view class="card">
    <view class="title">🎯 校园贷风险评估</view>
    <view class="intro">
      <text class="text">欢迎参加校园贷风险评估！请回答以下问题，我们将为您提供个性化的风险评估报告。</text>
    </view>
  </view>

  <!-- 评估进度 -->
  <view class="card">
    <view class="subtitle">📊 评估进度</view>
    <view class="progress-container">
      <view class="progress-bar" style="width: {{progress}}%;"></view>
    </view>
    <view class="progress-text">
      <text class="text">{{currentQuestion + 1}}/{{questions.length}}</text>
    </view>
  </view>

  <!-- 题目卡片 -->
  <view class="card question-card">
    <view class="question-title">
      <text class="text">{{currentQuestion + 1}}. {{questions[currentQuestion].question}}</text>
    </view>
    <view class="options">
      <view class="option-item" wx:for="{{questions[currentQuestion].options}}" wx:key="index" bindtap="selectOption" data-index="{{index}}">
        <view class="option-radio {{selectedOption === index ? 'selected' : ''}}"></view>
        <text class="text">{{item}}</text>
      </view>
    </view>
  </view>

  <!-- 答题按钮 -->
  <view class="card">
    <view class="btn-container">
      <button class="btn-primary" bindtap="submitAnswer" disabled="{{selectedOption === null}}">提交答案</button>
      <button class="btn-warning" bindtap="skipQuestion" disabled="{{currentQuestion === questions.length - 1}}">跳过此题</button>
      <button class="btn-danger" bindtap="viewResult" disabled="{{currentQuestion !== questions.length - 1}}">查看结果</button>
    </view>
  </view>

  <!-- 评估结果 -->
  <view class="card result-card" wx:if="{{showResult}}">
    <view class="title">📈 您的风险评估报告</view>
    <view class="risk-score">
      <text class="score-text">{{riskScore}}</text>
      <text class="score-label">分</text>
    </view>
    <view class="risk-level">
      <text class="text">风险等级：{{riskLevelText}}</text>
    </view>
    <view class="risk-description">
      <text class="text">{{riskDescription}}</text>
    </view>
    <view class="risk-suggestions">
      <view class="subtitle">💡 风险防范建议</view>
      <view class="suggestions">
        <view class="suggestion-item" wx:for="{{riskSuggestions}}" wx:key="index">
          <text class="text">{{item}}</text>
        </view>
      </view>
    </view>
    <view class="btn-container">
      <button class="btn-primary" bindtap="restartAssessment">重新评估</button>
      <button class="btn-warning" bindtap="shareResult">分享报告</button>
    </view>
  </view>
</view>
